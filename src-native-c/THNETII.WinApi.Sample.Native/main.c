#include <WinSock2.h>

int main(int argc, char* argv[])
{
    u_char instance;
    const int size = sizeof(instance);
    const int value = WINSOCK_VERSION;

    const void* ptr = NULL;

    return EXIT_SUCCESS;
}
