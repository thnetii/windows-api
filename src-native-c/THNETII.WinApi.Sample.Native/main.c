#include <WinSock2.h>
#include <afunix.h>

int main(int argc, char* argv[])
{
    IN_ADDR instance;
    const int size = sizeof(instance);
    const int value = UNIX_PATH_MAX;

    const void* ptr = NULL;

    return EXIT_SUCCESS;
}
