#include <Windows.h>

int main(int argc, char* argv[])
{
    void* instance;
    const int size = sizeof(instance);
    const int value = ERROR_SUCCESS;

    const void* ptr = NULL;

    return EXIT_SUCCESS;
}
