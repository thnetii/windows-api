#include <AccCtrl.h>

int main(int argc, char* argv[])
{
    PVOID instance;
    const int size = sizeof(instance);
    const int value = NULL;

    const void* ptr = AccFree;

    return EXIT_SUCCESS;
}
