#include <Windows.h>
#include <winscard.h>

int main(int argc, char* argv[])
{
    DWORD instance;
    const int size = sizeof(instance);
    const int value = SCARD_S_SUCCESS;

    const void* ptr = NULL;

    return EXIT_SUCCESS;
}
