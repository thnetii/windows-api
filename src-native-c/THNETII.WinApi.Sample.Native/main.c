#include <Windows.h>

#include <winerror.h>

int main(int argc, char* argv[])
{
    void* instance;
    const int size = sizeof(instance);
    const int value = ERROR_NOT_FOUND;

    const void* ptr = NULL;

    return EXIT_SUCCESS;
}
