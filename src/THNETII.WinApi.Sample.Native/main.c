#include <stdlib.h>
#include <stdio.h>

#include <Windows.h>
#include <LM.h>

void main()
{
	const int size = sizeof(HRESULT);
	const int value = NETSETUP_PROVISIONING_PARAMS_CURRENT_VERSION;

	void* ptr = NULL;
}
