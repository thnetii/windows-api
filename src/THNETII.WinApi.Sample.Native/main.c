#include <Windows.h>

#include <NtDsAPI.h>

int main(int argc, char* argv[])
{
    const int size = sizeof(DS_REPL_NEIGHBORSW);
    const int value = DS_REPSYNC_ABANDONED;

    DS_REPL_NEIGHBORSW instance;

    return EXIT_SUCCESS;
}
